{
  "name": "svelte-networked-store",
  "version": "0.1.0",
  "description": "The SvelteNetworkedStore provides a custom svelte stores that allows you to fetch ressources and handle an internal state (loading, fetched, error, ...).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "npx tsc",
    "test": "npx mocha --reporter spec --require ts-node/register test/**/*.test.ts"
  },
  "keywords": [
    "Svelte",
    "Typescript"
  ],
  "author": "Hendrik Eisenblätter",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "svelte": "^3.49.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}